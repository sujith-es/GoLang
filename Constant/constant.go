package main

import (
	"fmt"
)

func main() {
	const name = "Sujith"
	const course = "Docker deep dive"

	fmt.Println("\nHi", name, " You're currently watching", "'", course, "'", " which is constant.")
}
